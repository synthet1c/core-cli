const program = require('commander')

program
  .command('contribute', 'Install a module')
  .command('install', 'Install a module')
  .command('list', 'Install a module')
  .command('symbolic-ref', 'Install a module')
  .command('update', 'Install a module')
